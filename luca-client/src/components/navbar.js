import styled from "styled-components";

const Navigator = styled.div`
  width: 100vw;
  height: 10vh;
  background-color: cyan;
  text-align: center;
`;

export default function Navbar() {
  return <Navigator>this is Navbar</Navigator>;
}
